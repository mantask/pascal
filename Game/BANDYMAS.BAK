uses graph, taik;
 var gm, gd : integer;
     x, y: integer;
     status : Tstatus;
begin
initgraph (gm, gd, '');


{x := 100; y := 100;

procedure kulka (x, y : integer);
begin
  setcolor (8);
  setfillstyle (1, 8);
  pieslice (x + 2, y, 0, 180, 2);
  Bar (x, y, x + 4, y + 4);
  setcolor (7);
  setfillstyle (1, 7);
  Bar (x, y + 5, x + 4, y + 22);
end;
 }
status.paruosk;
status.piesk;


repeat until port [$60] = 1;
closegraph;
end.